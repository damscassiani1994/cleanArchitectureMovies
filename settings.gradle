/*
 * This file was generated by the Gradle 'init' task.
 *
 * The settings file is used to specify which projects to include in your build.
 *
 * Detailed information about configuring a multi-project build in Gradle can be found
 * in the user manual at https://docs.gradle.org/6.7/userguide/multi_project_builds.html
 */

rootProject.name = 'CleanArchitecture_Test'

FileTree buildFiles = fileTree(rootDir) {
    List excludes = gradle.startParameter.projectProperties.get("excludeProjects")?.split(",")
    include '**/*.gradle'
    exclude 'applications/admin-server', 'main.gradle','build', '**/gradle', 'settings.gradle', 'buildSrc', '/build.gradle', '.*', 'out'
    if(excludes) {
        exclude excludes
    }

}

println 'A message which is logged at QUIET level' + buildFiles

String rootDirPath = rootDir.absolutePath + File.separator
println rootDirPath
buildFiles.each { File buildFile ->

    boolean isDefaultName = 'build.gradle'.equals(buildFile.name)
    if(isDefaultName) {

        String projectName = buildFile.parentFile.name
        String projectPath = ':' + projectName
        include projectPath
        def project = findProject("${projectPath}")
        project.name = buildFile.parentFile.parentFile.name + '-' + projectName
        project.projectDir = buildFile.parentFile
        project.buildFileName = buildFile.name
        println 'Projects for build' + project

    } else {
        String projectName = buildFile.name.replace('.gradle', '')
        String projectPath = ':' + projectName
        include projectPath
        def project = findProject("${projectPath}")
        project.name = projectName
        project.projectDir = buildFile.parentFile
        project.buildFileName = buildFile.name
        println 'Projects NOT for build' + project
    }
}
